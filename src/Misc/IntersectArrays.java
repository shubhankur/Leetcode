package Misc;

public class IntersectArrays {
    public static int[] intersect(int[] nums1, int[] nums2) {
        int small;
        if(nums1.length < nums2.length){
            small = nums1.length;
        }
        else small = nums2.length;
        int[] result = new int[small];
        for(int i =0;i<small;i++){

        }
        return nums1;
    }
}
